# OCR engine: "tesseract" (default, fully offline) or "ocrspace" (cloud fallback)
OCR_ENGINE=tesseract

# Required only when OCR_ENGINE=ocrspace
# OCR_API_KEY=your_ocrspace_api_key

# Port the backend listens on (default 3001)
PORT=3001
